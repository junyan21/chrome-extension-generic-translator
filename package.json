{
  "name": "claude-translator",
  "version": "1.0.0",
  "description": "DeepL-style Chrome extension for Japanese-English translation using Claude API",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.251",
    "esbuild": "^0.19.8",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0"
  }
}
